# N-step returns ablation configuration
# Tests: 1 (standard TD), 3 (more Monte Carlo-like)

defaults:
  - default

# Hydra sweeper for n-step grid search
hydra:
  sweeper:
    params:
      agent.n_step: 1, 3
      seed: 1, 2, 3
