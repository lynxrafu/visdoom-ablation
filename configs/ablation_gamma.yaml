# Discount factor (gamma) ablation configuration
# Tests: 0.9 (more immediate rewards), 0.99 (long-term planning)

defaults:
  - default

# Hydra sweeper for gamma grid search
hydra:
  sweeper:
    params:
      agent.gamma: 0.9, 0.99
      seed: 1, 2, 3
